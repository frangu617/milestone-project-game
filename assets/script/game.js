let canvas = document.getElementById("gameCanvas");
let context = canvas.getContext("2d");
let gameUI = canvas.getContext("2d");

let score = 0;

gameUI.font = "10px Arial";
gameUI.fillText("Score: 0", 10, 10);

// Create a square object

function update() {

}

function updateScore() {
    gameUI.clearRect(10, 0, canvas.width - 10, 10);
    score++
    // Set the font and draw the new score
    gameUI.font = "10px Arial";
    gameUI.fillText(`Score: ${score}`, 10, 10);
}
// Start the game loop
setInterval(update, 100);
//initialize canvas
let canvas = document.getElementById("gameCanvas");
let context = canvas.getContext("2d");

const scale = window.devicePixelRatio;
canvas.width = Math.floor(window.innerWidth * .50);
canvas.height = Math.floor(window.innerHeight * .50);

context.scale(scale *2, scale *2);

//end initialize canvas



// Update the game state
function update() {
updateScore();
animate();
}
//end update game state



//functions that alter the game without input
function updateScore(number) {
    context.clearRect(10, 0, canvas.width - 5, 10);
    if (number <= 0 || number > 0) {
    score+=number
}
    // Set the font and draw the new score
    context.font = "20px Arial";
    context.fillText(`Score: ${score}`, 10, 20);
}
// Start the game loop
setInterval(update, 10);

// Create a square object
function animate(){
    context.clearRect(0, 20, canvas.width, canvas.height);
    context.drawImage(img, canvas.width / 4, canvas.height / 4);
// x-=4;
    if (x > 250) requestAnimationFrame(animate);
}

//end functions that alter the game without input



// some variables
let score = 0;
let x = canvas.width;
let y = 0;
let img = new Image();

img.onload = animate;
img.src = "assets/pngs/blueSquare.png";

//end variables

